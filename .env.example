# Server-side only. Provider keys must never reach browser code — the app
# talks to /api/routing/* and Nitro talks to the provider.

# Optional. Better for Czech and Central European addresses.
# Without it the app falls back to keyless OSRM/OpenStreetMap.
MAPY_API_KEY=
